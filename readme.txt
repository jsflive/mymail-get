Example MyMail GET is a Maven project. To build it execute:

mvn clean install

The example can be run on Tomcat 7. This can be done via the IDE or by directly deploying the war file created by the Maven build.